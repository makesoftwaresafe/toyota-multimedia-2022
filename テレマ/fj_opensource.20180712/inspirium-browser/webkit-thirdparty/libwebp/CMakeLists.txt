CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

REMOVE_DEFINITIONS(-DHAVE_CONFIG_H=1)
INCLUDE(Platform${PORT}.cmake)

ADD_SUBDIRECTORY(src)

# TODO: for unix port
IF("${PORT}" STREQUAL "Unix")
ELSEIF("${PORT}" STREQUAL "Android")
ELSE()
	ADD_SUBDIRECTORY(examples)
ENDIF()

