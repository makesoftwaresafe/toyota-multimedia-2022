#ifndef __GENERATED_BOUNDS_H__
#define __GENERATED_BOUNDS_H__

#define MAX_NR_ZONES 2

/*
#define __GENERATING_BOUNDS_H

struct page;

static inline int SetPageActive(struct page* pg)
{
    return 0;
}

static inline int PageActive(struct page* pg)
{
    return 0;
}

static inline int PageUnevictable(struct page* pg)
{
    return 0;
}

static inline int PageReferenced(struct page* pg)
{
    return 0;
}

static inline int PageLRU(struct page* pg)
{
    return 0;
}

static inline int ClearPageReferenced(struct page* pg)
{
    return 0;
}

static inline int SetPageReferenced(struct page* pg)
{
    return 0;
}

static inline int ClearPageReclaim(struct page* pg)
{
    return 0;
}

static inline int PageDirty(struct page* pg)
{
    return 0;
}

static inline int TestSetPageDirty(struct page* pg)
{
    return 0;
}

static inline int PageTail(struct page* pg)
{
    return 0;
}

static inline int PageSlab(struct page* pg)
{
    return 0;
}

static inline int PageAnon(struct page* pg)
{
    return 0;
}

static inline int PageHeadHuge(struct page *page)
{
    return 0;
}

static inline int PageHead(struct page *page)
{
    return 0;
}
*/
#endif /* __GENERATED_BOUNDS_H__ */

#if defined(__QNXNTO__) && defined(__USESRCVERSION)
#include <sys/srcversion.h>
__SRCVERSION("$URL: http://svn.ott.qnx.com/product/graphics/branches/denso-jp-hv-ivi/hardware/gpu/drm/server-gp12-gold/include/generated/bounds.h $ $Rev: 836322 $")
#endif
